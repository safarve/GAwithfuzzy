You can run code using:
python main.py

Certain parts are commented by default, check comments before running these parts.

dependencies -- numpy,scipy,fylearn 

Results_synopsis -- Have details on parameters used and accuracy of the neural network.
resultsGAplusFuzzy -- Has a set of the format {Predicted,actual} per line for the test data
terminal-- Screenshot of the result from main.py

EnglishHnd -- from the dataset
Preprocessed -- From the code, this is already created so don't change.

Do not change the folder structure or filenames, since they are used inside the code.